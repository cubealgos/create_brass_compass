<!-- generated by tools/map.py; edit the source, then run `just map` -->

# `brass_compass.network` — root

Every type with its summary and every non-private constructor, method and constant. The
signature is the contract; read the source only when the summary is not enough.

The one client-to-server packet (docs/spec/domains/ui.md, UI-REQ-007): save an entry for the held compass.

### `record SavePayload(InteractionHand hand, String dimension, BlockPos pos, String name)` — `src/main/java/brass_compass/network/SavePayload.java`
Save an entry: the place, the dimension the client believes it is in, and the name.
- `Type<SavePayload> TYPE`
- `StreamCodec<ByteBuf, SavePayload> STREAM_CODEC`
- `Type<? extends CustomPacketPayload> type()`
- `boolean apply(ServerPlayer player, SavePayload payload)` — The server's handling; returns whether the entry was written.

