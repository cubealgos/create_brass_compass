---
title: "create_brass_compass DEC-006 — Destinations as a versioned data component on the item; two screens on Create Fly's GUI framework; renames as one validated packet"
type: "spec"
category: "create_brass_compass"
---

# `DEC-006` — Destinations as a versioned data component on the item; two screens on Create Fly's GUI framework; renames as one validated packet

**Status:** decided by Kevin, 2026-09-19.

Follows ARCH-DEC-002 and ARCH-DEC-003. Alternatives: a server-side registry keyed by item UUID (rejected: an item has no identity in vanilla, and copies would share). Cost if wrong: large components on heavily used compasses.
